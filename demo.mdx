---
title: Demo
description: Live interactive component example

---

import { useState } from 'react'

# Live Code Demo

```jsx live
function Counter() {
  const [count, setCount] = useState(0)
  return (
    <div>
      <h2>Count: {count}</h2>
      <button onClick={() => setCount(count + 1)}>Increment</button>
    </div>
  )
}
export default Counter
```